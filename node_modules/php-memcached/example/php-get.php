<?php
$db=new Memcached();
$db->addServer('127.0.0.1',11211);

// json_decode(val, false);
//$db->setOption(Memcached::OPT_SERIALIZER, Memcached::SERIALIZER_JSON);

// json_decode(val, true);
$db->setOption(Memcached::OPT_SERIALIZER, Memcached::SERIALIZER_JSON_ARRAY);

for($i=1; $i<8; $i++) {
	echo 'a'.$i.': ';
	var_dump($db->get('a'.$i));
};
